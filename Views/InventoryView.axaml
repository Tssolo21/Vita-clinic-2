<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="using:Avalonia.Controls"
             xmlns:animation="clr-namespace:Avalonia.Animation;assembly=Avalonia.Animation"
             x:Class="VitaClinic.WebAPI.Views.InventoryView">

    <UserControl.Styles>
        <!-- Search Field Focus Animation -->
        <Style Selector="TextBox.search-field">
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Inventory Card Hover Effect -->
        <Style Selector="Border.inventory-card">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BoxShadow" Value="0 2 8 #0000001A"/>
        </Style>
    </UserControl.Styles>

    <StackPanel Margin="20">
        <!-- Header with animation -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="📦" FontSize="32" Margin="0,0,12,0" VerticalAlignment="Center"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Inventory Management" FontSize="28" FontWeight="Bold" Foreground="#1F2937"/>
                <TextBlock Text="Manage clinic supplies and medications" FontSize="14" Foreground="#6B7280" Margin="0,4,0,0"/>
            </StackPanel>
        </StackPanel>

        <!-- Search and Filter Section -->
        <Border Background="#F9FAFB" CornerRadius="12" Margin="0,0,0,20" Padding="20" Classes="card">
            <StackPanel>
                <!-- Search Field -->
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Name="SearchBox" Grid.Column="0"
                             Watermark="Search inventory by item name, type, or supplier..."
                             Classes="search-field"
                             Padding="16,12"
                             CornerRadius="8"
                             FontSize="14"
                             Margin="0,0,12,0"/>

                    <Button Grid.Column="1" Content="🔍 Search" Background="#6366F1" Foreground="White"
                            Padding="16,12" CornerRadius="8" Classes="card"/>
                </Grid>

                <!-- Filter Buttons -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock Text="Quick Filters:" VerticalAlignment="Center" Margin="0,0,12,0"
                               Foreground="#6B7280" FontWeight="Bold"/>
                    <Button Name="AllItemsFilter" Content="All Items" Classes="filter-button"
                            Padding="12,8" Margin="0,0,8,0" CornerRadius="6"/>
                    <Button Name="LowStockFilter" Content="Low Stock" Classes="filter-button"
                            Padding="12,8" Margin="0,0,8,0" CornerRadius="6"/>
                    <Button Name="MedicineFilter" Content="Medicine" Classes="filter-button"
                            Padding="12,8" Margin="0,0,8,0" CornerRadius="6"/>
                    <Button Name="SuppliesFilter" Content="Supplies" Classes="filter-button"
                            Padding="12,8" CornerRadius="6"/>
                </StackPanel>

                <!-- Search Results Info -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                    <TextBlock Name="ResultsCount" Text="Showing 25 items" FontSize="12" Foreground="#6B7280"/>
                    <TextBlock Text="•" Margin="8,0" Foreground="#D1D5DB"/>
                    <TextBlock Name="FilterStatus" Text="No filters applied" FontSize="12" Foreground="#6B7280"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Action Buttons -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
            <Button Name="BackButton" Content="⬅️ Back to Dashboard" Click="GoBack"
                    Background="#6B7280" Foreground="White" Padding="12,8" Margin="0,0,10,0"
                    Classes="card"/>
            <Button Name="AddInventoryButton" Content="➕ Add New Item" Click="AddInventoryItem"
                    Background="#6366F1" Foreground="White" Padding="12,8" Margin="0,0,10,0"
                    Classes="card"/>
            <Button Name="RefreshButton" Content="🔄 Refresh" Click="LoadInventory"
                    Background="#10B981" Foreground="White" Padding="12,8"
                    Classes="card"/>
        </StackPanel>

        <!-- Inventory Items List -->
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <ItemsControl Name="InventoryList" ItemsSource="{Binding InventoryItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#E5E7EB" BorderThickness="2" CornerRadius="12" Margin="8"
                                Padding="20" Background="White" Classes="inventory-card">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Item Icon -->
                                <Border Grid.Column="0"
                                        Width="64" Height="64"
                                        CornerRadius="32"
                                        Background="#F3F4F6"
                                        Margin="0,0,16,0"
                                        VerticalAlignment="Top"
                                        HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding ItemType, StringFormat='📦'}" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <!-- Item Content -->
                                <StackPanel Grid.Column="1">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                        <TextBlock Text="Item #" FontSize="12" FontWeight="Bold" Foreground="#6B7280"/>
                                        <TextBlock Text="{Binding Id}" FontSize="12" FontWeight="Bold" Foreground="#374151" Margin="4,0,12,0"/>
                                        <!-- Low Stock Badge -->
                                        <Border Background="{Binding Quantity, Converter={StaticResource LowStockConverter}}" CornerRadius="12" Padding="8,4" Margin="8,0,0,0">
                                            <TextBlock Text="{Binding Quantity, StringFormat='Stock: {0}'}" FontSize="10" Foreground="#166534" FontWeight="Bold"/>
                                        </Border>
                                    </StackPanel>

                                    <TextBlock Text="{Binding ItemName}" FontSize="20" FontWeight="Bold"
                                               Foreground="#1F2937" Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding ItemType}" FontSize="16" FontWeight="SemiBold"
                                               Foreground="#374151" Margin="0,0,0,8"/>

                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                                <TextBlock Text="📊" FontSize="12" Margin="0,0,4,0"/>
                                                <TextBlock Text="Quantity:" FontSize="12" FontWeight="Bold" Foreground="#6B7280"/>
                                                <TextBlock Text="{Binding Quantity}" FontSize="12" Foreground="#374151" Margin="4,0,0,0"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="📉" FontSize="12" Margin="0,0,4,0"/>
                                                <TextBlock Text="Min Stock:" FontSize="12" FontWeight="Bold" Foreground="#6B7280"/>
                                                <TextBlock Text="{Binding MinimumStockLevel}" FontSize="12" Foreground="#374151" Margin="4,0,0,0"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1" Margin="16,0,0,0">
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                                <TextBlock Text="💰" FontSize="12" Margin="0,0,4,0"/>
                                                <TextBlock Text="Unit Price:" FontSize="12" FontWeight="Bold" Foreground="#6B7280"/>
                                                <TextBlock Text="{Binding UnitPrice, StringFormat=C}" FontSize="12" Foreground="#374151" Margin="4,0,0,0"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🏢" FontSize="12" Margin="0,0,4,0"/>
                                                <TextBlock Text="Supplier:" FontSize="12" FontWeight="Bold" Foreground="#6B7280"/>
                                                <TextBlock Text="{Binding Supplier}" FontSize="12" Foreground="#374151" Margin="4,0,0,0"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>

                                    <!-- Description -->
                                    <TextBlock Text="{Binding Description}" FontSize="12" Foreground="#6B7280" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                </StackPanel>

                                <!-- Action Buttons -->
                                <StackPanel Grid.Column="2" Orientation="Vertical" VerticalAlignment="Top" Margin="16,0,0,0">
                                    <Button Content="✏️ Edit" Padding="12,8" Margin="0,0,0,8"
                                            Background="#6366F1" Foreground="White" CornerRadius="8"
                                            Click="EditInventoryItem" Tag="{Binding Id}"
                                            Classes="card"/>
                                    <Button Content="🗑️ Delete" Padding="12,8"
                                            Background="#EF4444" Foreground="White" CornerRadius="8"
                                            Click="DeleteInventoryItem" Tag="{Binding Id}"
                                            Classes="card"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </StackPanel>
</UserControl>